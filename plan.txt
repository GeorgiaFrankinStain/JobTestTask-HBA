? тип данных для денег ?
    разумеется int, а вдруг 

? делать ли проще устройство кредитный лимит, помещая всю инфу в одну переменную ?
    ? может ли счет на дебитовой карте уходить меньше 0 ?

    ? делать ли в дочернем классе "CreditCard" отдельную переменную ?
        ? делать ли перегрузку операторов возвращения баланса из родительского класса и использовать переменную родительского класса или сделать свою переменую ?
            КОД ДОЛЖЕН БЫТЬ ПРОСТЫМ И ПОНЯТНЫМ. Плохо использовать переменную, которая изначально не задумывалась для такого поведения И называется не подходящим образом.
            В данном конкретном случае я бы лучше создал отдельный интерфейс и отедльные реализации, но раз в задании предпологается абстрактный класс, то видимо будет много общего функционала.

            логика поведения должна быть в дочерних классах, дабы избежать глупых ошибок

+ долг можно показывать в виде отрицательного баланса ?
    нет, судя по примерам




? оставить возможность добавить 0 денег ?
    по идее стоит, вдруг будут из коллекции что-то добавлять. Чисто логически ничего плохого не произойдет, зато не придется пользователям моего класса писать обработчик такой ситуации
